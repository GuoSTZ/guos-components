import React from 'react';
import { Loading } from 'guos-components';

const App = () => {
  return (
    <div style={{ width: 'fit-content', background: '#000' }}>
      <Loading />
    </div>
  );
};

export default App;
